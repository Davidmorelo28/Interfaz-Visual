import React from 'react'
import ReactDOM from 'react-dom/client'
import HidroBot from './HidroBot'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <HidroBot />
  </React.StrictMode>,
)
